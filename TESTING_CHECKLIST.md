# Чеклист для тестирования после рефакторинга

## ✅ Что было сделано

1. ✅ Удален старый `database.py` (536 строк)
2. ✅ Создана новая структура `database/` с модулями по доменам
3. ✅ Создана система сессий `sessions/`
4. ✅ Создан слой сервисов `services/`
5. ✅ Обновлены все handlers для работы с новой структурой
6. ✅ Добавлена поддержка обработки фото и голосовых сообщений (структура готова)

## 🧪 Что нужно протестировать

### Базовые команды
- [ ] `/start` - приветствие
- [ ] `/help` - справка
- [ ] `/dayresult` - просмотр результатов за день

### Работа с едой
- [ ] Отправка текстового сообщения о еде (например: "Гороховый суп и 2 яблока")
- [ ] Проверка, что бот правильно распознал блюда и посчитал КБЖУ
- [ ] Проверка, что записи сохраняются в базу данных

### Редактирование
- [ ] Нажатие кнопки "Редактировать" на сообщении с едой
- [ ] Отправка изменений (например: "Исправь на 300 ккал")
- [ ] Проверка, что изменения применились
- [ ] Нажатие кнопки "Отменить" во время редактирования

### Удаление
- [ ] Нажатие кнопки "Удалить" на сообщении с едой
- [ ] Проверка, что запись удалилась из базы данных
- [ ] Проверка, что сообщение удалилось из чата

### Дни
- [ ] Команда `/nextday` - создание следующего дня
- [ ] Проверка, что после `/nextday` новые записи идут в новый день
- [ ] Проверка автоматического создания нового дня в 4:00 утра (если возможно)

### Часовой пояс
- [ ] Команда `/timezone` без аргументов - просмотр текущего часового пояса
- [ ] Команда `/timezone Europe/Moscow` - установка часового пояса
- [ ] Команда `/timezone Invalid/Timezone` - проверка обработки ошибки

### Медиа (структура готова, но обработка пока не реализована)
- [ ] Отправка фото - должно прийти сообщение о том, что обработка фото пока не реализована
- [ ] Отправка голосового сообщения - должно прийти сообщение о том, что обработка голоса пока не реализована

## 🔍 Что проверить в логах

- [ ] Нет ошибок импорта модулей
- [ ] База данных инициализируется корректно
- [ ] Сессии работают правильно (проверить логи при редактировании)
- [ ] Нет ошибок при сохранении/обновлении/удалении записей

## 📝 Если что-то не работает

1. Проверьте логи бота (`bot.log` или вывод в консоль)
2. Убедитесь, что все зависимости установлены: `pip install -r requirements.txt`
3. Проверьте, что файл `.env` существует и содержит `TELEGRAM_TOKEN` и `GIGACHAT_AUTH_KEY`

## 🎯 Ожидаемое поведение

Все функции должны работать так же, как до рефакторинга. Изменения касаются только внутренней структуры кода, пользовательский опыт не изменился.
